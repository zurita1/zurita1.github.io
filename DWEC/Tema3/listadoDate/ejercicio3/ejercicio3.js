{
    function init() {
        info=document.getElementById("p")
        info.innerHTML = "El método parse Transforma una cadena con la representación de una fecha y hora, y devuelve el número de milisegundos";        
    }
    window.addEventListener("load", init);
}